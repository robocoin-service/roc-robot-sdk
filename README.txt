ROC Robot SDK

Linux TPM robot recommended one-step install:

curl -fsSL https://raw.githubusercontent.com/robocoin-service/roc-robot-sdk/main/install.sh | ROC_SERVER_URL=http://172.16.18.187:8090 bash -s -- <sdkBindingToken>

Manual command after clone:

./roc-robot-tpm-sdk.sh bind <sdkBindingToken> <serverUrl>

The SDK package is generic. The binding token is robot-specific and generated by the platform after robot registration.
The SDK reads TPM public key data, asks the TPM private key to sign the challenge payload, and submits the signed report to roc-server.
After binding succeeds, the SDK starts the DeRAS Agent and waits for platform stage verification challenges.
The TPM private key is non-exportable and will not be written to the database.
